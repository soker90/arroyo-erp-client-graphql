/**
 * Options for currency type columns in euros
 * @type {{maximumFractionDigits: number, minimumFractionDigits: number, locale: string, currencyCode: string}}
 */
export const currencySetting = {locale: 'es-ES', currencyCode: 'EUR', minimumFractionDigits: 0, maximumFractionDigits: 2};