import styled from 'styled-components';

const styles = theme => ({
  progress: {
    margin: theme.spacing.unit * 2,
  },
});

export default styles;

export const StyledRefinanced = styled.div`
  padding: 0 0.5rem;
  background: #428bca;
  color: white;
  margin-bottom: 0.5rem;
  display: flex;
  justify-content: center;
  font-weight: 600;
`;
